import Layout from "../components/Layout.server";

export default function HomeTest() {
    return (
        <Layout>
            <h1>Acá va la home Gabi. El Layout que envuelve lo que hace es ponerte el header y el footer, si se lo sacas te queda blanca la pagina</h1>
        </Layout>
    )
}